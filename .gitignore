node_modules/
.next/
data/
*.db
*.db-journal
*.db-wal
*.db-shm
# Secrets — never commit these (AUTH_PASSWORD etc.)
.env
.env.*
.env*.local
next-env.d.ts

# Build artifacts
tsconfig.tsbuildinfo

# Claude Code local state (machine-specific permissions and launch config)
.claude/

# Cloudflare tunnel client — a 40MB platform binary, not source. Download it for
# the host that needs it: https://github.com/cloudflare/cloudflared/releases
cloudflared
